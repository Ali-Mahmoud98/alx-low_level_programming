#include "main.h"

/**
  * _abs - function takes one param
  * @a: number to get its absolute
  * value.
  * Return: the absolute value of a
  */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	return (-a);
}
